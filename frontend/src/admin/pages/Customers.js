import {admin} from '../services/api.js';
export default function Customers(){setTimeout(async()=>{try{const d=await admin.customers();const el=document.querySelector('#customers-table');if(el)el.innerHTML=(d.items||[]).map(x=>`<tr><td>${x.name||'—'}</td><td>${x.mobile}</td><td>${x.orders||0}</td><td>${new Intl.NumberFormat('fa-IR').format(x.total_purchase_irt||0)}</td></tr>`).join('')||'<tr><td colspan="4">مشتری وجود ندارد.</td></tr>'}catch(e){document.querySelector('#customers-error').textContent=e.message}},0);return `<div class="admin-page" dir="rtl"><div class="admin-title"><h2>مشتریان</h2></div><div id="customers-error" class="error"></div><div class="panel"><table class="admin-table"><thead><tr><th>نام</th><th>موبایل</th><th>سفارش</th><th>خرید</th></tr></thead><tbody id="customers-table"><tr><td colspan="4">در حال دریافت...</td></tr></tbody></table></div></div>`;}