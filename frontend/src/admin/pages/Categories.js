import {api} from '../services/api.js';
export default function Categories(){setTimeout(async()=>{await load();document.querySelector('#cat-form')?.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/api/admin/categories',{method:'POST',body:JSON.stringify({name:f.get('name'),slug:f.get('slug'),description:f.get('description')})});e.currentTarget.reset();await load()}catch(x){alert(x.message)}})},0);
async function load(){try{const d=await api('/api/admin/categories');document.querySelector('#cat-table').innerHTML=(d.items||[]).map(x=>`<tr><td>${x.name}</td><td>${x.slug}</td><td>${x.active?'فعال':'غیرفعال'}</td><td><button class="btn danger" data-id="${x.id}">حذف</button></td></tr>`).join('')||'<tr><td colspan="4">دسته‌ای نیست.</td></tr>';document.querySelectorAll('#cat-table .danger').forEach(b=>b.onclick=async()=>{try{await api('/api/admin/categories/'+b.dataset.id,{method:'DELETE'});load()}catch(e){alert(e.message)}})}catch(e){document.querySelector('#cat-error').textContent=e.message}}
return `<div class="admin-page" dir="rtl"><div class="admin-title"><h2>دسته‌بندی‌ها</h2></div><div id="cat-error" class="error"></div><div class="panel"><form id="cat-form" class="form"><div class="form-grid"><label>نام دسته<input name="name" required></label><label>Slug<input name="slug" required></label></div><label>توضیح<textarea name="description"></textarea></label><button class="btn primary">ثبت دسته</button></form></div><div class="panel"><table class="admin-table"><thead><tr><th>نام</th><th>Slug</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody id="cat-table"></tbody></table></div></div>`}