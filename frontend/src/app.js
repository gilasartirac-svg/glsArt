const API=window.GILASART_API||'https://gilasartworker.gilasart-ir-ac.workers.dev';
let csrfToken='';
const app=document.querySelector('#app');let state={products:[],categories:[],user:null,roles:[],cart:null};
const fa=n=>new Intl.NumberFormat('fa-IR').format(Number(n||0));
const THEME_KEY='gilasart-theme';
function applyTheme(theme){const t=theme==='light'?'light':'dark';document.documentElement.dataset.theme=t;try{localStorage.setItem(THEME_KEY,t)}catch{}}
function initTheme(){let t='';try{t=localStorage.getItem(THEME_KEY)||''}catch{}applyTheme(t||(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark'))}
window.GilasArtTheme={toggle(){applyTheme(document.documentElement.dataset.theme==='light'?'dark':'light')},set:applyTheme};
initTheme();
async function api(path,opt={}){const headers={...(opt.headers||{})};if(opt.body)headers['content-type']='application/json';const r=await fetch(API+path,{credentials:'include',cache:'no-store',headers,...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'خطا');return d}
function csrf(){return csrfToken||''}
function setSeo({title,description,image,type='website',jsonLd}={}){if(title){document.title=title;let t=document.querySelector('meta[name="description"]');if(!t){t=document.createElement('meta');t.name='description';document.head.appendChild(t)}t.content=description||'';const og=document.querySelector('meta[property="og:title"]');if(og)og.content=title;const od=document.querySelector('meta[property="og:description"]');if(od)od.content=description||'';if(image){let oi=document.querySelector('meta[property="og:image"]');if(!oi){oi=document.createElement('meta');oi.setAttribute('property','og:image');document.head.appendChild(oi)}oi.content=image}}document.querySelectorAll('script[data-gilasart-jsonld]').forEach(x=>x.remove());if(jsonLd){const s=document.createElement('script');s.type='application/ld+json';s.dataset.gilasartJsonld='1';s.textContent=JSON.stringify(jsonLd);document.head.appendChild(s)}}
function isAdminUser(){return state.user?.mobile==="09153090907"}
function layout(content){app.innerHTML=`<header class="top"><div class="wrap nav"><a class="brand" href="#/">گیلاس آرت<small>GILAS ART</small></a><nav class="links" aria-label="منوی اصلی"><a href="#/">خانه</a><a href="#/shop">فروشگاه</a><a href="#/about">درباره ما</a>${isAdminUser()?'<a class="admin-link" href="#/admin">کنترل پنل</a>':''}</nav><div class="spacer"></div><button class="theme-toggle" type="button" aria-label="تغییر حالت نمایش" title="روز / شب" onclick="window.GilasArtTheme&&window.GilasArtTheme.toggle()">◐ <span>روز/شب</span></button><a class="iconbtn" href="#/cart">سبد</a><a class="iconbtn" href="#/account">حساب</a></div></header><main>${content}</main><footer class="footer"><div class="wrap">گیلاس آرت — تجربه‌ای آرام برای خرید هنر.</div></footer>`}
function productCard(p){return `<article class="card"><a href="#/product/${encodeURIComponent(p.slug)}"><div class="thumb">${p.image?`<img src="${p.image}" alt="${p.name}" loading="lazy">`:'اثر هنری'}</div><div class="cardbody"><h3>${p.name}</h3><div class="muted">${p.sku||''}</div><div class="price">${fa(p.price_irt)} ریال</div></div></a></article>`}
function startSkyAnimation(){
 const canvas=document.querySelector('#sky-canvas');if(!canvas)return;
 const ctx=canvas.getContext('2d');let raf=0,t=0;
 const resize=()=>{const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=Math.max(1,r.width*d);canvas.height=Math.max(1,r.height*d);ctx.setTransform(d,0,0,d,0,0)};
 const stars=Array.from({length:55},(_,i)=>({x:(i*83)%100,y:(i*47)%72,r:i%3===0?1.4:.8,a:.25+(i%5)*.1}));
 const frame={x:62,y:51,w:210,h:150};
 function draw(){
  t+=.004;const r=canvas.getBoundingClientRect(),w=r.width,h=r.height;ctx.clearRect(0,0,w,h);
  const sky=ctx.createLinearGradient(0,0,0,h);sky.addColorStop(0,'#081525');sky.addColorStop(.58,'#17395a');sky.addColorStop(1,'#d6a875');ctx.fillStyle=sky;ctx.fillRect(0,0,w,h);
  for(const s of stars){ctx.globalAlpha=s.a*(.65+.35*Math.sin(t*5+s.x));ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(s.x/100*w,s.y/100*h,s.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;
  for(let i=0;i<4;i++){const x=((i*280-w*.15)+(t*35)% (w+420))-210,y=35+i*48;ctx.fillStyle='rgba(255,255,255,.10)';ctx.beginPath();ctx.ellipse(x,y,120,22,0,0,Math.PI*2);ctx.ellipse(x+80,y+4,95,18,0,0,Math.PI*2);ctx.fill()}
  const drift=Math.sin(t*1.7)*18;ctx.save();ctx.translate(w*.52+drift,h*.46+Math.sin(t*1.2)*8);ctx.rotate(Math.sin(t)*.025);
  ctx.strokeStyle='rgba(255,245,220,.9)';ctx.lineWidth=2;ctx.shadowBlur=24;ctx.shadowColor='rgba(255,225,160,.3)';ctx.strokeRect(-frame.w/2,-frame.h/2,frame.w,frame.h);ctx.shadowBlur=0;
  ctx.fillStyle='rgba(255,255,255,.035)';ctx.fillRect(-frame.w/2+8,-frame.h/2+8,frame.w-16,frame.h-16);ctx.restore();
  raf=requestAnimationFrame(draw)
 }
 resize();addEventListener('resize',resize,{passive:true});draw();canvas._cancel=()=>cancelAnimationFrame(raf)
}
async function home(){const [d,s]=await Promise.all([api('/api/products'),api('/api/settings').catch(()=>({settings:{}}))]);state.products=d.items||[];const ss=s.settings||{};setSeo({title:ss.seo_title||'گیلاس آرت | خرید تابلو و آثار هنری',description:ss.seo_description||ss.site_description});layout(`<section class="wrap hero"><div class="hero-copy"><div class="eyebrow">هنر، با انتخابی شخصی</div><h1>آثاری برای خانه‌هایی که داستان دارند.</h1><p>گیلاس آرت یک فروشگاه فارسی برای کشف و خرید آثار هنری است؛ از انتخاب تا پرداخت، ساده و مطمئن.</p><div class="toolbar"><a class="btn primary" href="#/shop">مشاهده آثار</a><a class="btn ghost" href="#/about">درباره گیلاس آرت</a></div></div><div class="hero-sky" aria-label="قاب هنری شناور در آسمان"><canvas id="sky-canvas"></canvas><div class="sky-caption"><span class="pill">GILAS ART</span><strong>هنر در حرکت</strong><small>یک قاب، یک آسمان، یک لحظه.</small></div></div></section><section class="wrap section"><div class="sectionhead"><h2>آخرین آثار</h2><a class="muted" href="#/shop">همه آثار ←</a></div><div class="grid">${state.products.slice(0,8).map(productCard).join('')||'<div class="panel">هنوز محصول فعالی منتشر نشده است.</div>'}</div></section>`);startSkyAnimation()}
async function shop(){const [p,c]=await Promise.all([api('/api/products'),api('/api/categories')]);state.products=p.items||[];state.categories=c.items||[];layout(`<section class="wrap page"><div class="sectionhead"><div><div class="eyebrow">CATALOG</div><h2>فروشگاه</h2></div></div><div class="toolbar"><input id="q" class="search" placeholder="جستجوی اثر، نام یا کد محصول"><button class="btn primary" id="search">جستجو</button>${state.categories.map(x=>`<button class="btn ghost cat" data-id="${x.id}">${x.name}</button>`).join('')}</div><div id="results" class="grid">${state.products.map(productCard).join('')}</div></section>`);document.querySelector('#search').onclick=async()=>{const q=document.querySelector('#q').value;const d=await api('/api/products?q='+encodeURIComponent(q));document.querySelector('#results').innerHTML=(d.items||[]).map(productCard).join('')||'<div class="panel">نتیجه‌ای پیدا نشد.</div>'};document.querySelectorAll('.cat').forEach(b=>b.onclick=async()=>{const d=await api('/api/products?category='+b.dataset.id);document.querySelector('#results').innerHTML=(d.items||[]).map(productCard).join('')})}
async function product(slug){const d=await api('/api/products/'+encodeURIComponent(slug));const p=d.product;setSeo({title:p.seo_title||p.name+' | گیلاس آرت',description:p.seo_description||p.description,image:p.image?new URL(p.image,location.href).href:undefined,jsonLd:{'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description||'',sku:p.sku,image:p.image?[new URL(p.image,location.href).href]:[],offers:{'@type':'Offer',priceCurrency:'IRR',price:String(p.price_irt),availability:'https://schema.org/InStock',url:location.href}}});layout(`<section class="wrap page product"><div class="visual">${p.image?`<img src="${p.image}" alt="${p.name}" style="max-width:100%;max-height:560px;object-fit:contain">`:'اثر هنری'}</div><div><span class="pill">${p.category_name||'اثر هنری'}</span><h1>${p.name}</h1><p class="muted">${p.description||''}</p><div class="price" style="font-size:24px;margin:24px 0">${fa(p.price_irt)} ریال</div><div class="toolbar"><button class="btn primary" id="add">افزودن به سبد</button><button class="btn ghost" id="fav">ذخیره</button></div><div class="panel"><h3>نظر خریداران</h3>${(d.reviews||[]).map(r=>`<p><b>${r.name||'خریدار'}</b> — ${'★'.repeat(r.rating)}<br>${r.body}</p>`).join('')||'<span class="muted">هنوز نظری ثبت نشده است.</span>'}</div></div></section>`);document.querySelector('#add').onclick=async()=>{try{await ensureLogin();await api('/api/cart',{method:'POST',body:JSON.stringify({productId:p.id,quantity:1}),headers:{'x-csrf-token':csrf()}});alert('به سبد اضافه شد')}catch(e){alert(e.message)}};document.querySelector('#fav').onclick=async()=>{try{await ensureLogin();await api('/api/favorites',{method:'POST',body:JSON.stringify({productId:p.id}),headers:{'x-csrf-token':csrf()}});alert('ذخیره شد')}catch(e){alert(e.message)}}}
async function ensureLogin(){if(state.user)return;await account();if(!state.user)throw new Error('ابتدا وارد حساب شوید')}
async function loadMe(){try{const d=await api('/api/me');state.user=d.user;state.roles=d.roles||[];csrfToken=d.csrfToken||csrfToken}catch{state.user=null}}
async function cart(){await loadMe();if(!state.user){layout(`<section class="wrap page"><div class="panel"><h2>سبد خرید</h2><p>برای دیدن سبد خرید وارد حساب شوید.</p><a class="btn primary" href="#/account">ورود</a></div></section>`);return}const d=await api('/api/cart');state.cart=d;layout(`<section class="wrap page"><h1>سبد خرید</h1><div class="panel">${(d.items||[]).map(x=>`<div class="cartline"><div class="grow"><b>${x.name}</b><div class="muted">${fa(x.price_irt)} × ${x.quantity}</div></div><button class="btn ghost del" data-id="${x.product_id}">حذف</button></div>`).join('')||'<p class="muted">سبد شما خالی است.</p>'}<hr><div class="sectionhead"><b>جمع کالاها</b><strong class="price">${fa(d.subtotal_irt)} ریال</strong></div>${d.items?.length?`<a class="btn primary" href="#/checkout">ادامه و ثبت سفارش</a>`:''}</div></section>`);document.querySelectorAll('.del').forEach(b=>b.onclick=async()=>{await api('/api/cart?productId='+b.dataset.id,{method:'DELETE',headers:{'x-csrf-token':csrf()}});cart()})}
async function account(){await loadMe();if(state.user){layout(`<section class="wrap page"><div class="panel"><h1>حساب کاربری</h1><p>${state.user.mobile}</p><p>${state.user.name||'کاربر گیلاس آرت'}</p><button class="btn" id="logout">خروج</button></div></section>`);document.querySelector('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST',headers:{'x-csrf-token':csrf()}});state.user=null;location.hash='/'};return}layout(`<section class="wrap page"><div class="panel" style="max-width:460px;margin:auto"><h1>ورود به گیلاس آرت</h1><p class="muted">کد یک‌بارمصرف برای شماره موبایل شما ارسال می‌شود.</p><div class="form"><label>شماره موبایل<input id="mobile" inputmode="numeric" placeholder="09123456789"></label><button class="btn primary" id="send">ارسال کد</button><div id="step"></div></div></div></section>`);document.querySelector('#send').onclick=async()=>{try{const d=await api('/api/auth/request-otp',{method:'POST',body:JSON.stringify({mobile:document.querySelector('#mobile').value})});document.querySelector('#step').innerHTML='<label>کد تایید<input id="otp" inputmode="numeric" maxlength="6"></label><button class="btn primary" id="verify">تایید</button>';document.querySelector('#verify').onclick=async()=>{try{const v=await api('/api/auth/verify-otp',{method:'POST',body:JSON.stringify({challengeId:d.challengeId,code:document.querySelector('#otp').value})});csrfToken=v.csrfToken||csrfToken;await loadMe();location.hash='/account'}catch(e){alert(e.message)}}}catch(e){alert(e.message)}}}
async function checkout(){await loadMe();if(!state.user){location.hash='/account';return}const idempotencyKey=(crypto.randomUUID?crypto.randomUUID():Date.now().toString(36)+Math.random().toString(36).slice(2)).replace(/-/g,'');const d=await api('/api/cart');layout(`<section class="wrap page"><div class="product"><div class="panel"><h1>ثبت سفارش</h1><div class="form"><label>نام گیرنده<input id="rn" value="${state.user.name||''}"></label><label>استان<input id="pr"></label><label>شهر<input id="ct"></label><label>نشانی<textarea id="ad"></textarea></label><label>کد پستی<input id="pc"></label><button class="btn primary" id="order">ثبت سفارش و پرداخت</button><div id="msg"></div></div></div><div class="panel"><h2>خلاصه</h2><p>جمع کالاها: ${fa(d.subtotal_irt)} ریال</p><p>ارسال: ${fa(d.subtotal_irt>=10000000?0:500000)} ریال</p><strong class="price">مبلغ نهایی: ${fa(d.subtotal_irt+(d.subtotal_irt>=10000000?0:500000))} ریال</strong></div></div></section>`);document.querySelector('#order').onclick=async()=>{try{const a=await api('/api/addresses',{method:'POST',body:JSON.stringify({recipientName:document.querySelector('#rn').value,province:document.querySelector('#pr').value,city:document.querySelector('#ct').value,address:document.querySelector('#ad').value,postalCode:document.querySelector('#pc').value}),headers:{'x-csrf-token':csrf()}});const o=await api('/api/orders',{method:'POST',body:JSON.stringify({addressId:a.addressId,idempotencyKey}),headers:{'x-csrf-token':csrf()}});const pay=await api('/api/orders/'+o.orderId+'/pay',{method:'POST',headers:{'x-csrf-token':csrf()}});location.href=pay.url}catch(e){document.querySelector('#msg').innerHTML='<span class="error">'+e.message+'</span>'}}}
async function router(){const p=location.hash.slice(2).split('/');try{if(!p[0])return home();
if(p[0]==='admin'){const base=location.pathname.includes('/glsArt/')?'/glsArt':'';const {default:AdminApp}=await import(base+'/admin/AdminApp.js');await loadMe();if(!isAdminUser()){location.hash='/account';return}app.innerHTML=AdminApp();return}
if(p[0]==='shop')return shop();if(p[0]==='product')return product(p[1]);if(p[0]==='cart')return cart();if(p[0]==='account')return account();if(p[0]==='checkout')return checkout();if(p[0]==='about'){layout('<section class="wrap page"><div class="panel"><h1>درباره گیلاس آرت</h1><p>گیلاس آرت برای عرضه و خرید آثار هنری با تمرکز بر تجربه‌ای آرام، شفاف و فارسی ساخته شده است.</p></div></section>');return}if(p[0]==='payment'){layout(`<section class="wrap page"><div class="panel"><h1>${p[1]==='success'?'پرداخت با موفقیت تایید شد':'پرداخت ناموفق بود'}</h1><p class="muted">${new URLSearchParams(location.hash.split('?')[1]||'').get('order')||''}</p><a class="btn primary" href="#/">بازگشت به فروشگاه</a></div></section>`);return}home()}catch(e){layout(`<section class="wrap page"><div class="panel"><h2>خطا</h2><p class="error">${e.message}</p></div></section>`)}}
window.addEventListener('hashchange',async()=>{await loadMe();router()});loadMe().then(router);