const API=window.GILASART_API||'https://gilasartworker.gilasart-ir-ac.workers.dev';
let csrfToken='';
const app=document.querySelector('#app');let state={products:[],categories:[],user:null,roles:[],cart:null};
const fa=n=>new Intl.NumberFormat('fa-IR').format(Number(n||0));
async function api(path,opt={}){const headers={...(opt.headers||{})};if(opt.body)headers['content-type']='application/json';const r=await fetch(API+path,{credentials:'include',headers,...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'خطا');return d}
function csrf(){return csrfToken||''}
function layout(content){app.innerHTML=`<header class="top"><div class="wrap nav"><a class="brand" href="#/">گیلاس آرت<small>GILAS ART</small></a><nav class="links"><a href="#/shop">فروشگاه</a><a href="#/about">درباره ما</a>${state.roles.includes("admin")?'<a href="#/admin">کنترل پنل</a>':''}</nav><div class="spacer"></div><a class="iconbtn" href="#/cart">سبد</a><a class="iconbtn" href="#/account">حساب</a></div></header><main>${content}</main><footer class="footer"><div class="wrap">گیلاس آرت — تجربه‌ای آرام برای خرید هنر.</div></footer>`}
function productCard(p){return `<article class="card"><a href="#/product/${encodeURIComponent(p.slug)}"><div class="thumb">${p.image?`<img src="${p.image}" alt="${p.name}">`:'اثر هنری'}</div><div class="cardbody"><h3>${p.name}</h3><div class="muted">${p.sku||''}</div><div class="price">${fa(p.price_irt)} تومان</div></div></a></article>`}
async function home(){const d=await api('/api/products');state.products=d.items||[];layout(`<section class="wrap hero"><div><div class="eyebrow">هنر، با انتخابی شخصی</div><h1>آثاری برای خانه‌هایی که داستان دارند.</h1><p>گیلاس آرت یک فروشگاه فارسی و مینیمال برای کشف و خرید آثار هنری است؛ از انتخاب تا پرداخت، ساده و مطمئن.</p><div class="toolbar"><a class="btn primary" href="#/shop">مشاهده آثار</a><a class="btn ghost" href="#/about">درباره گیلاس آرت</a></div></div><div class="hero-card"><span class="pill">COLLECT / CURATE / LIVE</span><h2>زیبایی در جزئیات است.</h2><p class="muted">قاب، رنگ، فضا و احساسی که بعد از دیدن یک اثر باقی می‌ماند.</p></div></section><section class="wrap section"><div class="sectionhead"><h2>آخرین آثار</h2><a class="muted" href="#/shop">همه آثار ←</a></div><div class="grid">${state.products.slice(0,8).map(productCard).join('')||'<div class="panel">هنوز محصول فعالی منتشر نشده است.</div>'}</div></section>`)}
async function shop(){const [p,c]=await Promise.all([api('/api/products'),api('/api/categories')]);state.products=p.items||[];state.categories=c.items||[];layout(`<section class="wrap page"><div class="sectionhead"><div><div class="eyebrow">CATALOG</div><h2>فروشگاه</h2></div></div><div class="toolbar"><input id="q" class="search" placeholder="جستجوی اثر، نام یا کد محصول"><button class="btn primary" id="search">جستجو</button>${state.categories.map(x=>`<button class="btn ghost cat" data-id="${x.id}">${x.name}</button>`).join('')}</div><div id="results" class="grid">${state.products.map(productCard).join('')}</div></section>`);document.querySelector('#search').onclick=async()=>{const q=document.querySelector('#q').value;const d=await api('/api/products?q='+encodeURIComponent(q));document.querySelector('#results').innerHTML=(d.items||[]).map(productCard).join('')||'<div class="panel">نتیجه‌ای پیدا نشد.</div>'};document.querySelectorAll('.cat').forEach(b=>b.onclick=async()=>{const d=await api('/api/products?category='+b.dataset.id);document.querySelector('#results').innerHTML=(d.items||[]).map(productCard).join('')})}
async function product(slug){const d=await api('/api/products/'+encodeURIComponent(slug));const p=d.product;layout(`<section class="wrap page product"><div class="visual">${p.image?`<img src="${p.image}" alt="${p.name}" style="max-width:100%;max-height:560px;object-fit:contain">`:'اثر هنری'}</div><div><span class="pill">${p.category_name||'اثر هنری'}</span><h1>${p.name}</h1><p class="muted">${p.description||''}</p><div class="price" style="font-size:24px;margin:24px 0">${fa(p.price_irt)} تومان</div><div class="toolbar"><button class="btn primary" id="add">افزودن به سبد</button><button class="btn ghost" id="fav">ذخیره</button></div><div class="panel"><h3>نظر خریداران</h3>${(d.reviews||[]).map(r=>`<p><b>${r.name||'خریدار'}</b> — ${'★'.repeat(r.rating)}<br>${r.body}</p>`).join('')||'<span class="muted">هنوز نظری ثبت نشده است.</span>'}</div></div></section>`);document.querySelector('#add').onclick=async()=>{try{await ensureLogin();await api('/api/cart',{method:'POST',body:JSON.stringify({productId:p.id,quantity:1}),headers:{'x-csrf-token':csrf()}});alert('به سبد اضافه شد')}catch(e){alert(e.message)}};document.querySelector('#fav').onclick=async()=>{try{await ensureLogin();await api('/api/favorites',{method:'POST',body:JSON.stringify({productId:p.id}),headers:{'x-csrf-token':csrf()}});alert('ذخیره شد')}catch(e){alert(e.message)}}}
async function ensureLogin(){if(state.user)return;await account();if(!state.user)throw new Error('ابتدا وارد حساب شوید')}
async function loadMe(){try{const d=await api('/api/me');state.user=d.user;state.roles=d.roles||[];csrfToken=d.csrfToken||csrfToken}catch{state.user=null}}
async function cart(){await loadMe();if(!state.user){layout(`<section class="wrap page"><div class="panel"><h2>سبد خرید</h2><p>برای دیدن سبد خرید وارد حساب شوید.</p><a class="btn primary" href="#/account">ورود</a></div></section>`);return}const d=await api('/api/cart');state.cart=d;layout(`<section class="wrap page"><h1>سبد خرید</h1><div class="panel">${(d.items||[]).map(x=>`<div class="cartline"><div class="grow"><b>${x.name}</b><div class="muted">${fa(x.price_irt)} × ${x.quantity}</div></div><button class="btn ghost del" data-id="${x.product_id}">حذف</button></div>`).join('')||'<p class="muted">سبد شما خالی است.</p>'}<hr><div class="sectionhead"><b>جمع کالاها</b><strong class="price">${fa(d.subtotal_irt)} تومان</strong></div>${d.items?.length?`<a class="btn primary" href="#/checkout">ادامه و ثبت سفارش</a>`:''}</div></section>`);document.querySelectorAll('.del').forEach(b=>b.onclick=async()=>{await api('/api/cart?productId='+b.dataset.id,{method:'DELETE',headers:{'x-csrf-token':csrf()}});cart()})}
async function account(){await loadMe();if(state.user){layout(`<section class="wrap page"><div class="panel"><h1>حساب کاربری</h1><p>${state.user.mobile}</p><p>${state.user.name||'کاربر گیلاس آرت'}</p><button class="btn" id="logout">خروج</button></div></section>`);document.querySelector('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});state.user=null;location.hash='/'};return}layout(`<section class="wrap page"><div class="panel" style="max-width:460px;margin:auto"><h1>ورود به گیلاس آرت</h1><p class="muted">کد یک‌بارمصرف برای شماره موبایل شما ارسال می‌شود.</p><div class="form"><label>شماره موبایل<input id="mobile" inputmode="numeric" placeholder="09123456789"></label><button class="btn primary" id="send">ارسال کد</button><div id="step"></div></div></div></section>`);document.querySelector('#send').onclick=async()=>{try{const d=await api('/api/auth/request-otp',{method:'POST',body:JSON.stringify({mobile:document.querySelector('#mobile').value})});document.querySelector('#step').innerHTML='<label>کد تایید<input id="otp" inputmode="numeric" maxlength="6"></label><button class="btn primary" id="verify">تایید</button>';document.querySelector('#verify').onclick=async()=>{try{const v=await api('/api/auth/verify-otp',{method:'POST',body:JSON.stringify({challengeId:d.challengeId,code:document.querySelector('#otp').value})});csrfToken=v.csrfToken||csrfToken;await loadMe();location.hash='/account'}catch(e){alert(e.message)}}}catch(e){alert(e.message)}}}
async function checkout(){await loadMe();if(!state.user){location.hash='/account';return}const idempotencyKey=(crypto.randomUUID?crypto.randomUUID():Date.now().toString(36)+Math.random().toString(36).slice(2)).replace(/-/g,'');const d=await api('/api/cart');layout(`<section class="wrap page"><div class="product"><div class="panel"><h1>ثبت سفارش</h1><div class="form"><label>نام گیرنده<input id="rn" value="${state.user.name||''}"></label><label>استان<input id="pr"></label><label>شهر<input id="ct"></label><label>نشانی<textarea id="ad"></textarea></label><label>کد پستی<input id="pc"></label><button class="btn primary" id="order">ثبت سفارش و پرداخت</button><div id="msg"></div></div></div><div class="panel"><h2>خلاصه</h2><p>جمع کالاها: ${fa(d.subtotal_irt)} تومان</p><p>ارسال: ${fa(d.subtotal_irt>=10000000?0:500000)} تومان</p><strong class="price">مبلغ نهایی: ${fa(d.subtotal_irt+(d.subtotal_irt>=10000000?0:500000))} تومان</strong></div></div></section>`);document.querySelector('#order').onclick=async()=>{try{const a=await api('/api/addresses',{method:'POST',body:JSON.stringify({recipientName:document.querySelector('#rn').value,province:document.querySelector('#pr').value,city:document.querySelector('#ct').value,address:document.querySelector('#ad').value,postalCode:document.querySelector('#pc').value}),headers:{'x-csrf-token':csrf()}});const o=await api('/api/orders',{method:'POST',body:JSON.stringify({addressId:a.addressId,idempotencyKey}),headers:{'x-csrf-token':csrf()}});const pay=await api('/api/orders/'+o.orderId+'/pay',{method:'POST',headers:{'x-csrf-token':csrf()}});location.href=pay.url}catch(e){document.querySelector('#msg').innerHTML='<span class="error">'+e.message+'</span>'}}}
async function router(){const p=location.hash.slice(2).split('/');try{if(!p[0])return home();

if(p[0]==='admin'){const base = location.pathname.includes('/glsArt/')
 ? '/glsArt'
 : '';

const {default:AdminApp}=await import(
 base + '/admin/AdminApp.js'
);

 await loadMe();

if(!state.roles){
 state.roles=[];
}

 if(!state.user||!state.roles.some(r=>['admin','super_admin','manager'].includes(r))){location.hash='/account';return;} app.innerHTML=AdminApp();
 return;
}
if(p[0]==='shop')return shop();if(p[0]==='product')return product(p[1]);if(p[0]==='cart')return cart();if(p[0]==='account')return account();if(p[0]==='checkout')return checkout();if(p[0]==='about'){layout('<section class="wrap page"><div class="panel"><h1>درباره گیلاس آرت</h1><p>گیلاس آرت برای عرضه و خرید آثار هنری با تمرکز بر تجربه‌ای آرام، شفاف و فارسی ساخته شده است.</p></div></section>');return}if(p[0]==='payment'){layout(`<section class="wrap page"><div class="panel"><h1>${p[1]==='success'?'پرداخت با موفقیت تایید شد':'پرداخت ناموفق بود'}</h1><p class="muted">${new URLSearchParams(location.hash.split('?')[1]||'').get('order')||''}</p><a class="btn primary" href="#/">بازگشت به فروشگاه</a></div></section>`);return}home()}catch(e){layout(`<section class="wrap page"><div class="panel"><h2>خطا</h2><p class="error">${e.message}</p></div></section>`)}}
window.addEventListener('hashchange',async()=>{await loadMe();router()});loadMe().then(router);
